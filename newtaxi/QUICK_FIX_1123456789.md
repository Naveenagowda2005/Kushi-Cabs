# QUICK FIX: Phone 1123456789 Stuck Registration

## Immediate Steps

### Step 1: Check Current Status

Run in Supabase SQL Editor:
```sql
-- Check if user exists in users table
SELECT id, phone, email, is_active, role_id, created_at 
FROM users 
WHERE phone = '1123456789';

-- Check if auth user exists (you'll see this in Supabase Dashboard > Auth > Users)
-- Search for: 1123456789@kushicabs.phone
```

### Step 2: Choose Your Fix Path

#### Path A: If user EXISTS in users table (profile is created)
✅ User is actually registered - just needs to log in
- They can go to Login screen and sign in with phone: 1123456789
- If they want to re-register, they need to first delete this account

#### Path B: If user does NOT exist in users table (stuck)
❌ Auth user exists but profile stuck - needs cleanup

Run this SQL:
```sql
-- Copy the auth user ID from Supabase Dashboard first
-- (Go to Auth > Users, find 1123456789@kushicabs.phone, note the ID)

-- Then update the profile (or it will be created on next registration attempt)
INSERT INTO users (
  id,                              -- Use the auth user ID
  phone,
  email,
  role_id,
  full_name,
  is_active,
  created_at
) VALUES (
  'AUTH_USER_ID_HERE',            -- PASTE the actual auth user UUID
  '1123456789',
  '1123456789@kushicabs.phone',
  2,                              -- 1=super_admin, 2=vendor, 3=driver (adjust as needed)
  'User From Stuck Registration',
  true,
  NOW()
)
ON CONFLICT (id) DO NOTHING;

-- Verify it worked
SELECT * FROM users WHERE phone = '1123456789';
```

**OR Delete Everything and Start Fresh:**

```sql
-- Delete from users table (this will cascade to wallets, drivers, vendors)
DELETE FROM users WHERE phone = '1123456789';

-- Then go to Supabase Dashboard > Auth > Users
-- Find: 1123456789@kushicabs.phone
-- Click the user, then click ⋮ (three dots) > Delete User
```

### Step 3: Verify Fix

- If kept user: They can immediately log in
- If deleted: Ask them to sign up again (should work now with improved backend)

## Why This Happens

1. User signs up → OTP verified
2. Backend creates auth account
3. App navigates to profile creation screen
4. Profile creation fails (network, validation, etc.)
5. **Now stuck**: Auth account exists but no profile in database

## Now Fixed

The improved backend (`/admin/create-driver-account`) now:
- ✅ Detects stuck registrations automatically
- ✅ Logs warnings for monitoring
- ✅ Returns `hasProfile: false` so app can handle it
- ✅ Allows recovery by retrying profile creation

## For Next Registration Attempt

If user tries to register again with same phone:
- Backend detects `hasProfile: false` 
- Knows user is in stuck state
- Allows retry to complete the profile

Next action: Update SignUpScreen to handle `hasProfile: false` response from backend.
